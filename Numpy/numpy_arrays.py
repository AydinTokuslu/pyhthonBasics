import numpy as np

# result=np.array([1,3,5,7,9])
#result=np.arange(1,10)# 1-10 arasında otomatik liste oluşturuyor. 
# result=np.arange(10,100,3)# 10-100 arasında 3 ere artan otomatik liste oluşturuyor. 

# result=np.zeros(10) #10 tane sıfır oluşturur
# result=np.ones(10) #10 tane bir oluşturur
# result=np.linspace(0,100,5) #10 tane bir oluşturur # 5 adet 0-100 arasında eşit sayı böler
# result=np.random.randint(0,10)#0-10 arasında rastgele sayı üretir.
# result=np.random.randint(20)#20'ye kadar rastgele sayı üretir.
# result=np.random.randint(0,10,3)#0-10 arasında rastgele 3 adet sayı üretir.
# result=np.random.rand(5)#0-5 arasında rastgele sayı üretir.
# np_array=np.arange(50)#0-50 arasında rastgele sayı üretir.
# np_multi=np_array.reshape(5,10)
# print(np_multi.sum(axis=1)) #axis=1 satırların toplamını verir
# print(np_multi.sum(axis=0)) #axis=0 sutunların toplamını verir

# rnd_numbers=np.random.randint(1,100,10)
# # result=rnd_numbers.max()
# result=rnd_numbers.min()
# result=rnd_numbers.mean() # üretilen sayıların ortalamasını alır.
# result=rnd_numbers.argmax() # üretilen en büyük sayının indexini getirir.
# result=rnd_numbers.argmin() # üretilen en küçük sayının indexini getirir.

print(result)